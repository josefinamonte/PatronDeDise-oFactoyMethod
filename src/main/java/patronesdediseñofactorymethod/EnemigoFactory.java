
package patronesdediseñofactorymethod;

/**
 *
A continuación, crearemos una interfaz llamada `EnemigoFactory` que contendrá 
el método factory para crear instancias de los diferentes tipos de enemigos:
 */

public interface EnemigoFactory {
   Enemigo crearEnemigo();
}
